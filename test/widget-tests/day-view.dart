import 'package:calorie_tracker_app/src/page/day-view/showDatePicker.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';
import 'package:calorie_tracker_app/src/services/shared_preference_service.dart';

void main() {
  // testWidgets(
  //     "Given this widget test is run"
  //     "When the ShowDatePicker widget is inflated"
  //     "Then the ShowDatePicker widget should render successfully",
  //     (WidgetTester tester) async {
  //   debugDefaultTargetPlatformOverride = TargetPlatform.android;

  //   await tester.pumpWidget(ShowDatePicker());

  //   expect(find.byType(ShowDatePicker), findsOneWidget);
  //   debugDefaultTargetPlatformOverride = null;
  // });

  // testWidgets(
  //     "Given this widget test is run"
  //     "When the Add Food button widget is tapped"
  //     "Then the Add Food Modal widget should render successfully",
  //     (WidgetTester tester) async {
  //   debugDefaultTargetPlatformOverride = TargetPlatform.android;

  //   await tester.pumpWidget(AddFoodButtonWidget());
  //   await tester.tap(find.byKey(ValueKey("add_food_modal_button")),
  //       warnIfMissed: true);

  //   // Future.delayed(const Duration(seconds: 2));
  //   // await tester.pump(new Duration(seconds: 2));
  //   await tester.pump();

  //   expect(find.byKey(ValueKey("add_food_modal")), findsOneWidget);
  //   debugDefaultTargetPlatformOverride = null;
  // });
}
